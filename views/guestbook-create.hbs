<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Create Guestbook Post</title>
</head>

<body>
    <h1>Create a Guestbook Post</h1>

    {{#if validationErrors.length}}
    <p>Errors:</p>
    <ul>
        {{#each validationErrors}}
        <li>{{this}}</li>
        {{/each}}
    </ul>
    {{/if}}

    <form action="/guestbook/create" method="POST">
        <fieldset>
            <label for="title">Name</label><br>
            <input type="text" id="input-name" name="name" value="{{name}}" placeholder="Name" class="form-input" /><br>
            <label for="content">Message</label><br>
            <textarea name="message" id="input-message" text="{{message}}" class="form-input" type="text" cols="30"
                rows="10"></textarea><br><br>
            <input type="hidden" name="_csrf" value="{{csrfToken}}">
            <input type="submit" value="Create Post" class="btn btn-primary">
        </fieldset>
    </form>
</body>